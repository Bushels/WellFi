# WellFi Marketing Page - Progress Log

This file is a current-state handoff, not a historical archive. Remove stale hero direction when the approved sequence changes.

## Current Parked Hero State

### Island Diorama Rendition

- Hero is the R3F island diorama, not the retired wave-sweep startup sequence.
- One signal-sending WellFi is shown inside the intermediate casing on the lower cased run.
- The cycle is a 12 s seamless loop: lit hold -> darken while staying wide -> three relay pulses -> relight while staying wide.
- The dark relay moment includes a surface telemetry bubble above the top of the intermediate casing:
  - Pressure `158 kPa`
  - Temperature `26 C`
  - Pump vibration `4.2 mm/s RMS`
- The bubble is an R3F `Html` overlay and must keep `data-wellfi-export-overlay` so MP4/GIF exports include it.
- Exported assets are under `exports/`; normal-speed and fast MP4/GIF pairs were generated on 2026-06-17.

### Branch Decision

- This rendition is merged to `master` for live review.
- Compare it against any remaining alternate renders before selecting the final public hero.

### Regression Triggers To Avoid

- Returning to two WellFi tools.
- Moving the active WellFi out of the intermediate casing.
- Dropping the telemetry bubble from the export path.
- Reintroducing a camera dolly/zoom-in for this wide-view version.
- Letting the telemetry bubble clip mobile viewports.
- Reintroducing scroll capture or canvas-only exports that hide HTML overlays.

## Current Calculator State

- The public `/calculator` route is parked as a contact handoff until benchmark assumptions and public claim boundaries are approved.
- Do not publish price, payout, uplift, run-life extension, or capital-efficiency claims without explicit approval and source notes.
- Keep the calculator code available for private candidate-well screening, but do not surface it publicly by default.
- Current public CTA should route to candidate-well review, not "first Canadian install" language.
