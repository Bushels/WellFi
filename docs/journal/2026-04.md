# April 2026 Journal

## 2026-04-30

First entry in this file.

- Started the cleanup pass after the hero calculator and proof-section work created enough local context to justify a project-state checkpoint.
- Confirmed the repo is on `codex/wellfi-hero-calculator` and the last verified commit is `4f820bf`, which locks the proof-section consolidation as the current known-good baseline.
- Checked `.gitignore` before adding anything. `.DS_Store` and `/coverage` were already covered, so the cleanup patch only adds the missing dev/serve log patterns.
- Confirmed `.claude/agents/` contains no absolute path references inside this project — agent specs are safe as-is, and moving `wellfi-info/` later would not break them.
- Read each of the 7 agent specs and built a lightweight routing manifest (`AGENTS.md`) rather than adding more process prose. The goal is fast handoff: which reviewer, when, where.
- Split Gemini guidance into a project-specific role file (`GEMINI.md`): creative direction, animation feel, and design critique only. Technical calls stay with Codex or Claude against the actual repo.
- Captured the current dirty tree in `PROJECT_STATE.md` so the next pass starts from evidence instead of memory.
- The active risk is hero animation direction drift — the approved sequence lives in `docs/hero-startup-animation.md` and must not be deviated from.
- Proposed stripping `CLAUDE.md` down to operating rules and durable project facts (deferred — REQUIRES KYLE APPROVAL). The cleanup target is less narrative, more contract.
- Parent directory `MPS/WellFi/` cleanup deferred to a separate Kyle-approval cycle. See cleanup audit for the proposed disposition table (delete/archive/absorb decisions per item).

## 2026-05-01

Continued the cleanup pass — Kyle approved Decisions A, B, and C from the prior audit.

- Applied the rules-only restructure of `CLAUDE.md`. Removed the `Quick Facts` narrative section and renamed the H1 from "Project Context" to "Rules". Stack info is auto-discoverable from `package.json`; audience info is duplicated inside `content-accuracy-verifier.md`. Hero copy refinements (`Know the Unknown`, `10,000 psia`) preserved. Bonus formatting cleanup: blank lines after every heading.
- Expanded `.gitignore` with broader hygiene patterns: OS artifacts (Thumbs.db, Desktop.ini), editor/IDE (.vscode/, .idea/, *.swp, *~), test outputs (/test-results/, /playwright-report/, /vitest-results/, /.nyc_output/). The originally-identified gap (dev-*.log, serve-*.log, start-*.log) shipped earlier in the same pass.
- Preserved the 4 Blender probe scripts (`inspect_materials.py`, `inspect_sideclamp_glb.py`, `orientation_probe.py`, `yaw_probe.py`) in `scripts/blender-probes/` with a README documenting purpose, hardcoded path dependencies, and a refactor note. Originally lived in the parent's `tmp/` scratch; moving them into the repo means the working knowledge survives the parent-dir cleanup.
- Parent directory `MPS/WellFi/` cleanup executed in three batches:
  - Batch 1 (loose files): `nul`, `dev-3005.err.log`, `dev-3005.out.log`, redundant `WellFi_Logo_V3.png` (byte-identical to `public/WellFi_Logo_V3.png`), and 15 hero/mobile/persistent iteration PNGs from the March 18 creative session.
  - Batch 2 (directories): `.next/` (build output in wrong dir), `.playwright-cli/` (~21 MB of console logs), `.playwright-mcp/`, `output/` (empty `logo-cleanup/`), `.agent/workflows/` (single legacy file), `Agent Instructions/` (7 legacy files — Feb 9 build-phase planning docs + Mar 28 superseded CLAUDE.md draft + agent-system snapshot).
  - Batch 3: entire `tmp/` (~79 MB) — probes already preserved; remainder was animation contact sheets, frame folders, redundant high-res PNG copies of reference photos, and stale `wellfi-dev.*.log`.
- Total disk reclaimed: ~111 MB. Parent now reduced from 30+ items to 9 — all of which have clear purpose (Blender source, CAD source, reference JPGs, product info folder, the marketing repo, plus session-level `.claude/` and `.mcp.json`).
- Held back two items intentionally:
  - `wellfi-info/` left untouched. Contains 4 customer-named analysis spreadsheets (Baytex, Headwater, Obsidian, Tamarack) and a `WellFi Customer Form.pdf`. Customer-confidential data does not belong in a GitHub-hosted marketing repo (`Bushels/WellFi`) regardless of public/private status. The 10 public-facing spec PDFs in there could be selectively absorbed into `wellfi-marketing/public/documents/` later (the directory exists and is empty), but that's a separate decision per file.
  - `blender/` left as-is. It's a 49 MB Blender working dir with the master `wellfi_hero_final.blend` (the source for the published hero webp images in `public/images/`). Stays in parent as creative scratch; not for version control due to size.
- The cleanup prompt's HARD STOP gate (gitignore audit) was clear from the start — `.claude/` was never being ignored — so the moves and deletions ran without remediation.
- Verification trail: every `rm` was preceded by either a Codex audit, an `ls` listing, or a checksum comparison (md5 match for the redundant logo). No deletions ran on the basis of the audit alone.
